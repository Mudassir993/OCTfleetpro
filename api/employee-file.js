const crypto = require('crypto');
const { Pool } = require('pg');
const { issueSignedToken, presignUrl, list } = require('@vercel/blob');
const pool = new Pool({connectionString:process.env.DATABASE_URL,max:5,idleTimeoutMillis:10000,connectionTimeoutMillis:10000,ssl:process.env.NODE_ENV==='production'?{rejectUnauthorized:false}:undefined});
function tokenHash(t){return crypto.createHash('sha256').update(t).digest('hex');}
function json(res,status,data){res.statusCode=status;res.setHeader('Content-Type','application/json');res.end(JSON.stringify(data));}
async function auth(req){const h=req.headers.authorization||'',raw=h.startsWith('Bearer ')?h.slice(7):null;if(!raw)return null;const c=await pool.connect();try{const r=await c.query(`SELECT u.id,u.username,u.name,u.role,u.status FROM app_sessions s JOIN app_users u ON u.id=s.user_id WHERE s.token_hash=$1 AND s.expires_at>now() AND u.status='Active'`,[tokenHash(raw)]);return r.rows[0]||null;}finally{c.release();}}
module.exports=async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'GET required'});try{const user=await auth(req);if(!user)return json(res,401,{error:'Authentication required'});const pathname=new URL(req.url,`http://${req.headers.host||'localhost'}`).searchParams.get('pathname')||'';if(!pathname.startsWith('employees/'))return json(res,400,{error:'Invalid pathname'});const c=await pool.connect();let doc;try{const r=await c.query('SELECT id,doc_key,name,content_type,pathname FROM employee_documents WHERE pathname=$1',[pathname]);doc=r.rows[0];}finally{c.release();}if(!doc)return json(res,404,{error:'Document not registered'});let actualPath=doc.pathname;try{const found=await list({prefix:doc.pathname,limit:20});if(found.blobs&&found.blobs.length){const exact=found.blobs.find(x=>x.pathname===doc.pathname);actualPath=exact?exact.pathname:found.blobs[0].pathname;}}catch(e){console.warn('Blob lookup fallback:',e.message);}const validUntil=Date.now()+5*60*1000;const signed=await issueSignedToken({pathname:actualPath,operations:['get'],validUntil});const result=await presignUrl(signed,{pathname:actualPath,operation:'get',validUntil,access:'private'});res.statusCode=302;res.setHeader('Location',result.presignedUrl);res.setHeader('Cache-Control','private, no-store');res.setHeader('Content-Disposition',`attachment; filename*=UTF-8''${encodeURIComponent(doc.name||'document')}`);return res.end();}catch(e){console.error('Employee Blob signed download error',e);return json(res,500,{error:e.message||'Could not create secure download URL'});}};
